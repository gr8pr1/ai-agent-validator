# Compiled BPF objects (regenerated by `make bpf`)
*.o
*.bpf.o

# Agent binary
agent/aiblocker-agent
agent/policyctl

# Local working state — internal source of truth, intentionally not published
state.md

# Audit logs / local runtime output
*.jsonl

# Editor / OS
.DS_Store
*.swp
